import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sign Up | Word Is Bond',
  description:
    'Create your Word Is Bond account. Start your 14-day free trial — evidence-grade call monitoring and AI analytics.',
}

export default function SignUpLayout({ children }: { children: React.ReactNode }) {
  return children
}
